@using EPiServer
@using EPiServer.Core
@using EPiServer.ServiceLocation
@using EPiServer.Web.Routing
@using PhonyClubDenmark.Website.Helpers

@{
    var _contentLoader = ServiceLocator.Current.GetInstance<IContentLoader>();
    var _currentContentLink = ViewContext.RequestContext.GetContentLink();
    var _menuPrimaryPages = EpiServerNavigationHelper.GetPageChildren(ContentReference.StartPage, onlyVisibleToMenu:true);
    var _startPage = _contentLoader.Get<PhonyClubDenmark.Website.Models.Pages.StartPage>(ContentReference.StartPage);
}
    
@* EMPTY FOR NOW *@

